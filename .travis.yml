language: python

script:
  - curl -s -X POST -H "Content-Type: application/json" -H "Accept: application/json" -H "Travis-API-Version: 3" -H "Authorization: token xxxxxx"  -d "request\": {\"branch\":\"master\"}}" https://api.travis-ci.com/repo/Dash-Industry-Forum%2FIdentifiers/requests

notifications:
  email:
    recipients:
      - emmanuel.thomas@tno.nl
      #- wilaw@akamai.com
    on_success: never # default: change
    on_failure: always # default: always
