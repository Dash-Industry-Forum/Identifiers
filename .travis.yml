language: python

script:
  - "curl -v -s -X POST -H 'Content-Type: application/json' -H 'Accept: application/json' -H 'Travis-API-Version: 3' -H 'Authorization: token $TRAVIS_TOKEN'  -d 'request\": {\"branch\":\"master\"}}' https://api.travis-ci.com/repo/edrthomas%2F/requests"

notifications:
  email:
    recipients:
      - emmanuel.thomas@tno.nl
      #- wilaw@akamai.com
    on_success: never # default: change
    on_failure: always # default: always
